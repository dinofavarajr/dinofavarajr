---
import Layout from '../../layouts/Layout.astro';
import Nav from '../../components/Nav.astro';
import Footer from '../../components/Footer.astro';
---

<Layout
  title="Sweet Fish Media Case Study | Dino Favara Jr."
  description="Produced 100–250 episodes and 250–500 short-form clips across multiple B2B podcast clients, generating 500K+ total views."
>
  <Nav />

  <main>
    <section class="max-w-3xl mx-auto px-6 pt-10">
      <a href="/work" class="inline-flex items-center gap-2 text-sm font-medium text-primary/70 hover:text-primary transition-colors duration-200">
        &larr; All case studies
      </a>
    </section>

    <section class="max-w-3xl mx-auto px-6 pt-8 pb-12">
      <span class="inline-block px-3 py-1 rounded-full text-xs font-medium bg-purple-500/10 text-purple-700 tracking-wide mb-6">
        Podcast Content Engine
      </span>
      <h1 class="font-display text-[clamp(2rem,5vw,3.2rem)] leading-[1.1] tracking-tight text-dark mb-4">
        Sweet Fish Media | B2B Podcast Production
      </h1>
      <p class="text-lg font-light text-dark/60 leading-relaxed max-w-xl">
        Produced 100–250 episodes and 250–500 short-form clips across multiple B2B podcast clients, generating 500K+ total views.
      </p>
      <div class="flex flex-wrap gap-2 mt-6">
        {['Podcast Strategy', 'Clip Selection', 'Content Briefs', 'QBR Reporting', 'Short-Form Distribution', 'Performance Analysis'].map(tag => (
          <span class="px-3 py-1.5 rounded-full border border-dark/10 bg-white/60 text-xs font-medium text-dark/55">{tag}</span>
        ))}
      </div>
    </section>

    <section class="stats-section max-w-3xl mx-auto px-6 pb-12">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
        {[
          { value: '100–250', label: 'Episodes Produced', sub: '' },
          { value: '250–500', label: 'Short-Form Clips Created', sub: '' },
          { value: '500K+', label: 'Total Views Generated', sub: '(approx.)' },
          { value: 'B2B', label: 'Niche Expertise', sub: '' },
        ].map(m => (
          <div class="rounded-xl border border-dark/8 bg-white/60 p-5 text-center">
            <span class="count-up font-display text-3xl text-primary" data-value={m.value}>{m.value}</span>
            <p class="mt-1 text-[11px] font-medium uppercase tracking-widest text-dark/45">{m.label}</p>
            {m.sub && <p class="mt-0.5 text-[10px] text-dark/35">{m.sub}</p>}
          </div>
        ))}
      </div>
    </section>

    <section class="max-w-3xl mx-auto px-6 pb-16">
      <article class="prose-custom">
        <h3>The Work</h3>
        <p>At Sweet Fish Media, I managed the full content lifecycle for multiple B2B podcast clients across different industries — from strategy and content briefs through clip selection, editing direction, distribution, and performance reporting.</p>

        <h3>What I Built</h3>
        <p><strong>Strategy & Ideation:</strong> I develop content briefs for each show, identifying which episodes and segments have the highest potential for short-form performance.</p>
        <p><strong>Clip Selection & Direction:</strong> For each episode, I identify the 3–5 moments with the strongest hook, emotional resonance, or tactical value. I write editing direction so editors can execute without guesswork.</p>
        <p><strong>Distribution & Optimization:</strong> Each clip is optimized for its target platform. A LinkedIn clip for a B2B SaaS podcast looks different than a YouTube Short for a marketing show.</p>
        <p><strong>Performance Reporting:</strong> I run quarterly business reviews (QBRs) with clients, analyzing what formats are working and adjusting strategy based on data. Not vanity metrics, but strategic conversations about what's driving results.</p>

        <h3>The Takeaway</h3>
        <p>Producing hundreds of episodes and clips across multiple clients simultaneously forced me to build systems that scale. I can't be in the weeds of every edit for every client. So I built repeatable frameworks for clip selection, editing direction, and performance analysis that work across niches. That same systems-thinking is what I bring to any content operation.</p>

        <p class="text-[11px] text-dark/35 mt-6">*Total views figure is approximate and being finalized.</p>
      </article>
    </section>

    <section class="max-w-3xl mx-auto px-6 pb-20">
      <div class="rounded-2xl border border-dark/8 bg-white/60 p-8 flex flex-col md:flex-row items-center justify-between gap-6">
        <div>
          <p class="font-display text-xl text-dark">Want results like this?</p>
          <p class="text-sm font-light text-dark/55 mt-1">Let's talk about building your content engine.</p>
        </div>
        <a href="mailto:dino@mercurysocialmedia.com" class="inline-flex items-center gap-2 px-7 py-3.5 rounded-full bg-primary text-off-white font-medium text-sm hover:bg-primary/90 hover:shadow-xl hover:shadow-primary/40 hover:-translate-y-0.5 transition-all duration-200 active:scale-95 whitespace-nowrap">
          Email me &rarr;
        </a>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

<script>
  function animateCount(el: HTMLElement) {
    const target = el.dataset.value ?? '';
    const hasPlus = target.includes('+');
    const hasM = target.includes('M');
    const hasK = target.includes('K');
    const hasPercent = target.includes('%');
    const hasX = /^\d+(\.\d+)?x$/.test(target.replace('+', ''));
    const clean = target.replace(/[+MKx%,\s]/gi, '').trim();
    if (!/^\d+\.?\d*$/.test(clean)) return;
    const raw = parseFloat(clean);
    if (isNaN(raw)) return;
    const dur = 5000;
    const start = performance.now();
    const fmt = (n: number): string => {
      if (hasM) return (Math.round(n * 10) / 10) + 'M' + (hasPlus ? '+' : '');
      if (hasK) return Math.round(n) + 'K' + (hasPlus ? '+' : '');
      if (hasPercent) return Math.round(n).toLocaleString() + '%';
      if (hasX) return Math.round(n) + 'x';
      return Math.round(n).toLocaleString() + (hasPlus ? '+' : '');
    };
    const tick = (now: number) => {
      const progress = Math.min((now - start) / dur, 1);
      const eased = 1 - Math.pow(1 - progress, 3);
      el.textContent = fmt(eased * raw);
      if (progress < 1) requestAnimationFrame(tick);
      else el.textContent = target;
    };
    el.textContent = fmt(0);
    requestAnimationFrame(tick);
  }
  const obs = new IntersectionObserver(entries => {
    entries.forEach(entry => {
      if (!entry.isIntersecting) return;
      entry.target.querySelectorAll<HTMLElement>('.count-up').forEach(animateCount);
      obs.unobserve(entry.target);
    });
  }, { threshold: 0.3 });
  document.querySelectorAll<HTMLElement>('.stats-section').forEach(el => obs.observe(el));
</script>

<style>
  .prose-custom h3 { font-family: 'Instrument Serif', serif; font-size: 1.35rem; font-weight: 400; color: var(--dark); margin-top: 2rem; margin-bottom: 0.75rem; }
  .prose-custom h3:first-child { margin-top: 0; }
  .prose-custom p { font-size: 0.95rem; font-weight: 300; line-height: 1.75; color: rgba(26, 21, 35, 0.65); margin-bottom: 1rem; }
  .prose-custom strong { font-weight: 500; color: var(--dark); }
</style>
